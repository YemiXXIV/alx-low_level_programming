#include <stdio.h>
/**
 ** main - Entry point of the program
 **
 ** Return; Always 0 (success)
 **/
int main(void)
{ int number = '0';
  char letter = 'a';
while (number < 10, letter <= 'f')
{putchar (number + '0');
number++; }
{putchar (letter); }
putchar ('\n');
return (0);
}
