#include "main.h"

/**
 * print_alphabet - function that prints all lower case
 *
 * Return: 0;
 */

void print_alphabet(void)
{ char c;
  for (c = 'a'; c <= 'z'; c++)
    { _putchar(c); }
  -putchar('\n'); }
